(function() {
    /**
     * @param z
     * @param {object} param
     * @param {number} param.x The horizontal axis
     * @param {number} param.y The vertical axis
    */

    function Coordinate1(z, { x, y }) {
        // ...
    }

    /**
     * @param {[{ x: { y: { z: number | string } } }, number]} param -
     * - `z`: Something here
     * - `y`: Something here too
    */

    function Coordinate2([{ x: { y: { z } } }, y]) {
        
    }
})()